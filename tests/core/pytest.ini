# 이 파일은 pytest에게 우리 프로젝트의 구조를 알려주는 역할을 합니다.
# 이를 통해 어디서 pytest를 실행하든 안정적으로 테스트를 찾을 수 있습니다.

[pytest]
# pytest가 테스트 파일을 찾아야 할 경로를 지정합니다.
testpaths = tests

# pytest가 소스 코드를 찾을 수 있도록 파이썬 경로에 현재 디렉토리(.)를 추가합니다.
# 'tests' 폴더에서 'secs_simulator'를 import 할 수 있게 해주는 핵심 설정입니다.
pythonpath = .
